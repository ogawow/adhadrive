import { AppRegistry } from 'react-native';
import App from '../App';

AppRegistry.registerComponent('AdhaDrive', () => App);
AppRegistry.runApplication('AdhaDrive', {
  rootTag: document.getElementById('root')
});
